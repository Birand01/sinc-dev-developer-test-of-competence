/** CRM dashboard content — stats from GET /api/dashboard (next). */
export function DashboardPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="text-muted-foreground mt-2 text-sm">
        Open chats, deals by stage, and owner breakdown — wired in the next
        step.
      </p>
    </div>
  )
}
