# Cloudflare Worker project config (like launchSettings + host config in .NET).
# Tells Wrangler which file is the entry point and which port to use locally.
name = "crm-api"
main = "src/index.ts"
compatibility_date = "2024-11-01"

[dev]
port = 8787
