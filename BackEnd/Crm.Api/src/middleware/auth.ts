/**
 * Auth middleware — validates Supabase JWT from Authorization: Bearer <token>.
 * Attaches user id and role to the request context for downstream handlers.
 * TODO: implement when Supabase is wired.
 */
export {};
