/**
 * Role guard middleware — allows only client | sales | manager (per route).
 * Works with auth middleware; returns 403 if role is not permitted.
 * TODO: implement when auth middleware exists.
 */
export {};
